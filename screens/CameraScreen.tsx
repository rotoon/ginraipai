import React from 'react'
import { View, Text } from 'dripsy'

const CameraScreen = () => (
  <View sx={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
    <Text sx={{ fontSize: 24 }}>Camera Screen (Placeholder)</Text>
  </View>
)

export default CameraScreen
